# Sales Intelligence Backend Memory

## Contents

- [Cloud deployment](#cloud-deployment)
- [2026-07-27 - Cloud Run database pooling repair](#2026-07-27---cloud-run-database-pooling-repair)
- [2026-07-27 - Frontend API repair and snapshot recovery](#2026-07-27---frontend-api-repair-and-snapshot-recovery)

## Cloud deployment

- Delivery rule: prove the simplest production-shaped user workflow before adding production-grade infrastructure. A deployment, healthy process, successful job execution, or zero-error exit is not evidence that scraping works. Do not call scraping complete unless a real supported source produces useful records, persists them, promotes them to `leads`, and the API shape used by the UI can retrieve them.
- The target architecture is a public FastAPI Cloud Run service plus an on-demand Cloud Run Job for Playwright scraping, both in GCP project `sales-intelligens` and region `europe-west1`.
- Supabase remains the managed PostgreSQL database. Runtime traffic must use the IPv4 Supavisor pooler with TLS; the direct `db.<project>.supabase.co` endpoint is IPv6-only in the current setup.
- The API scales from zero. The intended maximum was one instance, but live revision `sales-intelligence-api-00002-6r5` was audited with `autoscaling.knative.dev/maxScale=3`; reconcile that configuration explicitly instead of relying on this document. The worker executes one task at a time, uses one vCPU and 1 GiB RAM, and stops after 15 minutes.
- The Cloud Run deployment PR was merged to `main`. The July 17 repair work was performed directly against the updated local `main` worktree and remains uncommitted until explicitly requested.
- Job dispatch obtains the API service identity token from the Cloud Run metadata server and calls the Cloud Run v2 `jobs:run` endpoint. Dispatch failures mark the run failed instead of leaving it stuck in `running`; no GitHub token is part of this path.
- The worker now prefers one `DATABASE_URL` secret, while component-style PostgreSQL variables remain available to the existing scraper subprocesses. Railway/API credential removal remains the next test boundary.
- Worker completion now updates `scraper_runs` directly in PostgreSQL. The old Railway URL, hard-coded agent email/password, `requests` dependency, and authenticated HTTP callback have been removed from the worker execution path.
- The worker command builder no longer passes unsupported `--areas` arguments to the developer scraper; apartment and agency scrapers still receive the requested comma-separated areas.
- The worker reserves cleanup time inside the 15-minute Cloud Run limit by capping scraping at 12 minutes. Scraper or promotion subprocess failures now persist a failed run instead of reporting false success.
- `worker.Dockerfile` packages the Playwright runtime, scraper sources, promotion pipeline, and one-shot worker entrypoint. The root `.dockerignore` excludes `.env`, `.venv`, git metadata, tests, and local artifacts so secrets never enter Cloud Build context.
- `cloudbuild.worker.yaml` builds the worker with its non-default Dockerfile and publishes the image supplied through `_IMAGE`. Local `.env` now uses the IPv4 Supavisor session pooler with TLS; it remains git-ignored.
- Schema-contract tests now cover two stale boundaries found during deployment: listing promotion must target only current `leads` columns, and staging tables must contain the rating fields written by the Google Maps/developer scrapers.
- The pipeline listing promotion now matches the clean `leads` schema, and the canonical initialization adds the scraper rating fields. The same SQL enables RLS and removes `anon`/`authenticated` table and sequence grants because authorization belongs to FastAPI/JWT, not Supabase's Data API.
- The initial test import exposed an existing configuration defect: the default `ALLOWED_ORIGINS` value is a tuple because of a trailing comma. The deployment will fix that while retaining an explicit production origin allowlist.
- `ALLOWED_ORIGINS` is now a valid string default, and JWT signing fails closed through required `JWT_SECRET_KEY` settings rather than the repository's old insecure fallback value.
- Never commit database credentials, application secrets, or JWT keys. Store deployed values in GCP Secret Manager and keep local `.env` ignored.
- Cloud Run reserves `CLOUD_RUN_JOB`, so the API uses the application-specific `SCRAPER_CLOUD_RUN_JOB` environment variable for the worker job name.
- Deployment completed in GCP project `sales-intelligens`: the public API is `https://sales-intelligence-api-693887939589.europe-west1.run.app`. The earlier `sales-scraper` execution completed at the infrastructure level in 50.49 seconds but produced zero records; it did not prove scraping worked.
- A `$20` monthly project-scoped budget alert is configured. This is an alert, not a hard cap; the API is constrained to zero minimum and one maximum instance, while the worker is a single 15-minute task.
- The initial admin is `indrakenyadevelopment@gmail.com`; its temporary password is stored as `sales-admin-password` in GCP Secret Manager and must be changed on first login.
- Supabase is reachable through its TLS pooler. The canonical schema was applied idempotently, RLS is enabled, and direct `anon`/`authenticated` table and sequence grants are removed because access is mediated by the API.
- The July 15 clean-schema migration omitted `leads.notes` even though lead list, activity, timeline, detail, and update routes still use it. The canonical schema and SQLAlchemy model now include `notes TEXT`, and the additive migration was applied to Supabase.
- Cloud Run Job task retries are set to `1`. The scraper staging and promotion paths use unique constraints and `ON CONFLICT`, so one retry handles transient browser failures without intentionally duplicating leads.
- Sam (`samwelngugi24@gmail.com`) has Project Editor access. External Gmail Project Owner access must be invited from the Google Cloud Console and accepted by Sam; Google does not allow that owner invitation through `gcloud`.
- API revision `sales-intelligence-api-00002-6r5` uses image digest `sha256:2705e0226ac4e7f8480cdff2fbb328142273746aa0d6b5a3eb7669c27ccca59f`. Authentication and API-to-Job dispatch work, but the earlier developer smoke run only invoked enrichment against an empty `developer_staging` table and returned zero records. On 2026-07-17, the first real `agencies` / `Kilimani` scrape failed because the worker base image contained Playwright 1.60.0 while dependency resolution installed Playwright 1.61.0. Cloud Run retried the deterministic failure once.
- At the same audit point, the Cloud Run/Supabase path had zero leads while the still-active Railway backend reported 413 leads. The Vercel frontend still called Railway; Sam owns changing the Vercel environment setting. Do not claim cutover or parity until data migration and the UI-visible flow are verified.
- The real scraper failures had three concrete causes: the worker image used Playwright 1.60 while pip installed 1.61, Google redirected headless Cloud Run sessions to its consent page, and the live apartment staging table lacked `rating` and `review_count`. The package is now pinned to 1.60, all Google browser paths dismiss the consent page, the missing columns were added live and to the canonical migration, and child-process diagnostics are retained.
- A successful worker run must now produce at least one record. Each run writes its exact source rows and imported/duplicate/rejected outcome to `scraper_run_records`; final metrics are derived from those rows rather than a time-window query that mixed unrelated runs. This is the contract used by the frontend Record Audit drawer.
- Live proof on 2026-07-17: agency run 6 scraped and promoted 11 Kilimani agencies; apartment run 8 scraped 57 candidates and left 15 visible Thika apartment leads after promotion/deduplication; agency run 9 then returned 12 non-zero audit rows through `/api/scraper/runs/9/records` (1 imported, 11 duplicates). Run 9 used worker digest `sha256:f94f10914a323cf95a971722a7e24627c088b57bcf808665c07b6e7e07e688d2`.
- Frontend repo `gishusam/nyumba-lead-hub` actively uses login, `/api/scraper/run`, `/api/scraper/runs`, `/api/scraper/runs/{id}/records`, lead lists, dashboard summary/source counts, and weekly reports. Sam owns the Vercel `VITE_API_BASE_URL` change; backend validation must exercise those exact endpoints, not merely process health.
- Railway lead migration completed transactionally without an interactive Railway account login by using the existing direct database credential. The lossless merge moved 413 source leads into a target that already held 27, matched 12 genuine overlaps, and produced 428 leads plus all 10 notes and 6 events. Post-migration API evidence showed 428 total: 188 apartments, 179 agencies, 57 developers, and 4 landlords; workflow states preserved 6 called and 1 demo booked. Verified JSON snapshots are outside the repo at `C:\Users\akioko.INDRALIMITED\SalesIntelligenceMigrationBackups\20260717T185537Z`.
- The remaining Railway application data was migrated on 2026-07-17: both source users, 293 additional apartment staging records, 69 developer staging records, 99 Google Places records, all 23 Railway run-history rows, and all 134 Railway run-audit rows. Final Cloud Run counts are 3 users, 428 leads, 350 apartment staging rows, 69 developer staging rows, 111 Google Places rows, 32 run-history rows, and 146 run-audit rows. Sam's Railway user (`samwelngugi24@gmail.com`, sales role) was restored with the original Railway password hash, so the prior Railway password is the Cloud Run password. Backups are at `C:\Users\akioko.INDRALIMITED\SalesIntelligenceMigrationBackups\20260717T195104Z`.
- The legacy local agent no longer embeds the Railway database password or agent login. It requires `RAILWAY_DATABASE_URL`, `RAILWAY_AGENT_EMAIL`, and `RAILWAY_AGENT_PASSWORD` through the environment. Rotate the formerly committed Railway credential even though it has been removed from the current worktree and migration backups exist.
- Operational vocabulary: the Cloud Run service is the always-addressable receptionist for login, lists, starting work, and polling status; the Cloud Run Job is the one-off contractor that launches a browser, scrapes, saves, and exits. Scale-to-zero can create a cold-start delay, but no measured downtime claim exists yet.
- A reusable Google Cloud-style architecture diagram is generated by `docs/architecture/draw_architecture.py`; its standalone SVG, 1920x1080 PNG, README, and official Cloud Run/Build/Artifact Registry/Secret Manager icon assets live under `docs/architecture/`.

## 2026-07-27 - Cloud Run database pooling repair

- Decision and why: Cloud Run database traffic will use Supabase transaction mode on port `6543`; the FastAPI SQLAlchemy engine uses `NullPool` so its two Uvicorn processes cannot retain more sessions than the external pool permits.
- Changed surfaces: `backend/app/database.py` now builds engines without process-local pooling; `github_agent.py` retries only the database connection used to persist worker run status, with three bounded attempts. Scraper subprocess execution is not retried by this helper.
- Verification: the new red-green regression tests pass (`14 passed` across `tests/test_database_pooling.py` and `tests/test_worker.py`); `compileall` succeeds. The full suite remains `32 passed, 2 failed`, with the same two pre-existing BuyRentKenya fixture failures present before this change. Candidate run `4` completed with 12 UI audit rows. Production execution `sales-scraper-mb4j8` completed successfully in 1m2.68s using worker digest `sha256:9da805d3552b86885469caf3674d77aa0fb44775d5ed8f1d507913416acb267a`; production run `5` reports 12 found, 12 contacts, and 12 duplicates through both run and record-audit APIs.
- Production state: `sales-intelligence-api-00022-xux` receives 100% traffic and uses API digest `sha256:11a058cb9242514d1c1c72abbe5becc71b6a55be6f9715018cfcadd60f14a3ca`. Both API and `sales-scraper` use secret `sales-db-transaction-url` and port `6543`. Historical run `1` was repaired to `failed` with its original `EMAXCONNSESSION` cause; disposable zero-record candidate run `3` was deleted after exact identity and referential checks.
- Browser proof: headless agent-browser verified the Vercel Data Scraper page, production run history, terminal success status, and the 12-row duplicate audit without console/page errors. Screenshots are under `docs/assets/db-pooling-20260727/`, with `production-final-scraper.png` as the clean final state.
- Operational constraints / recovery: `sales-db-url` was not modified. API rollback is `gcloud run services update-traffic sales-intelligence-api --project sales-intelligens --region europe-west1 --to-revisions sales-intelligence-api-00018-pwz=100`. Worker rollback restores image tag `worker:c0f44f2`, `POSTGRES_PORT=5432`, and `DATABASE_URL=sales-db-url:latest`.
- Transaction-pool safety: never call session-level `set_session(readonly=True)` or set `default_transaction_read_only=on` through port `6543`; that state can leak to the next Supavisor client. Use a transaction-scoped `BEGIN READ ONLY` check or the session endpoint for maintenance. The validation-induced idle sessions were identified via `pg_stat_activity`, terminated only while idle, and a write-then-rollback check proved the pool read-write before rollout.
- Follow-up or explicit non-goals: SMTP, Groq, and other plaintext environment variables are intentionally unchanged pending the user's discussion with Sam. The selected Thika tile has a low-contrast black frontend style; it was observed but not changed in this backend repository.

## 2026-07-27 - Frontend API repair and snapshot recovery

- Production browser QA found three scoped frontend/API contract defects: the four lead-type tables called a missing `/api/leads/outreach` endpoint and remained stuck loading; the dashboard serialized TanStack Query's context object into `lead_type=[object Object]`; and password inputs omitted browser autocomplete metadata.
- The backend now exposes a static outreach route before `/api/leads`, with lead-type pagination, emailed/not-emailed counts, and latest successful email metadata. `/api/dashboard/by-area` applies its optional `lead_type` filter. Focused route and snapshot-recovery tests pass.
- The frontend calls `dashboardApi.byArea()` through an explicit zero-argument query function and rejects non-string filter values. Sign-in and password-change fields use the appropriate native autocomplete values. The production build and focused API-client regression test pass.
- Until the frontend deployment reaches production, the old bundle still sends `lead_type=[object Object]`. The backend therefore applies the area filter only for the four supported values (`apartment`, `agency`, `developer`, `landlord`) and ignores malformed legacy values so the production area chart does not empty during the staggered rollout.
- Main-database inspection proved real historical loss: only four July 27 scraper runs remained, versus 32 previously verified runs; lead notes/events were empty and historical lead rows were missing. The recovery used the verified July 17 JSON snapshots, matched existing entities conservatively, backed up all target tables, and inserted only missing data in one transaction.
- Post-recovery verified counts are 520 leads, 10 notes, 6 events, 36 scraper runs, and 182 run-audit rows. A repeat dry run matched all 428 historical snapshot leads and proposed zero further inserts. The pre-recovery backup is `C:\Users\akioko.INDRALIMITED\SalesIntelligenceMigrationBackups\20260727T154307Z`.
- Built-in-browser verification against the production-mode local frontend and fixed local API covered every top-level route with real Supabase data. There were zero console errors, failed requests, HTTP responses at or above 400, or stuck loading/error states. Evidence and the issue-by-issue justification are under `docs/assets/frontend-dogfood-20260727/`.
- The normal Vite development server emits a React hydration warning from Lovable's dev-only source tagger (`data-tsd-source` line metadata differs between SSR and client). Production excludes the tagger and was clean, so no application change was made for this tooling-only warning.
- Production deployment: Cloud Run revision `sales-intelligence-api-00026-lkr`, image digest `sha256:15940e8f7b6033262198a58b4a80ef213efb0d9d09923b5ec66cc6e6c3c2469d`, serves 100% traffic. Health and `/api/leads/outreach` OpenAPI checks passed. Rollback is `gcloud run services update-traffic sales-intelligence-api --project sales-intelligens --region europe-west1 --to-revisions sales-intelligence-api-00024-rbj=100`.
- The live Vercel frontend was rechecked after the API deployment. All ten top-level routes loaded; all eight outreach requests returned `200`; the legacy malformed dashboard filter returned `200` without emptying the chart; and the browser recorded zero console errors, failed requests, HTTP 4xx/5xx responses, or stuck loading/error states. Final production screenshots are in `docs/assets/frontend-dogfood-20260727/screenshots/`.
- The frontend PR merged, but Vercel's GitHub status points to a Sam-team invitation/access gate rather than a build result. Production remains operational through backend compatibility; Sam must grant the GitHub author/project team access or redeploy `main` from an authorized Vercel identity for the frontend-only cleanup and password autocomplete metadata to reach production.
- Production run `41` proved that the apartment scraper kept its PostgreSQL connection idle during several minutes of Playwright discovery/enrichment; Supabase's transaction pool closed that session before the first bulk write, producing `SSL SYSCALL error: EOF detected`. Scraper persistence is now operation-scoped: apartment, agency, and developer writes open a fresh transaction only when needed, prefer the single `DATABASE_URL` secret, close every connection immediately, and retry transient disconnects with a new connection. The writes are idempotent upserts/updates, so bounded retries cannot intentionally duplicate leads.
- The first fixed-image proof (`43`) confirmed that 68-69 Ruiru buildings could be saved without the SSL disconnect, then exposed an obsolete `conn.close()` left after the refactor; an AST regression test now prevents the main apartment workflow from retaining or referencing a long-lived connection. A second proof (`44`) exited cleanly and exposed that duplicate upserts did not refresh `scraped_at`, causing the UI to count only newly inserted rows. Apartment upserts now refresh `scraped_at`, matching the run audit to the actual processed set.
- Final production proof run `45` used worker digest `sha256:239e5dc375f0f7905153e8e467dbd160fbdd63597b0f4d6b6289602a899b52ff` and completed on its first attempt in 253 seconds: 65 Ruiru buildings found, 9 with contacts, 20 duplicates, 45 rejected, 65 UI audit rows, no stored error, zero Cloud Logging errors, and container exit code `0`. The Cloud Run Job keeps one retry and a 900-second task timeout; the scraper subprocess remains capped at 720 seconds.
- User-confirmed disposable local false starts `39` and `40` were deleted from production history after verifying both were failed. They had no dependent audit rows. Their recoverable JSON backup is `C:\Users\akioko.INDRALIMITED\SalesIntelligenceMigrationBackups\20260727T154307Z\deleted-false-starts-39-40.json`.

## Maintenance log

- 2026-07-17: Created this memory file for the initial Cloud Run deployment and Supabase security review.
- 2026-07-17: Completed Cloud Run API and worker deployment and verified API login/dispatch. The worker smoke execution produced zero records and was later determined to be a false positive, not a real scrape validation.
- 2026-07-17: Restored the missing `leads.notes` schema contract, redeployed the API, enabled one worker retry, added Sam as Project Editor, and verified API-to-worker execution end to end.
- 2026-07-17: Added and visually verified the programmatic Google Cloud architecture diagram and its reusable source assets.
- 2026-07-17: Corrected the deployment record after a real scrape audit: frontend remained on Railway, Cloud Run data was empty, and the worker had a Playwright image/package mismatch. Future completion requires non-zero, quality-checked, UI-retrievable output.
- 2026-07-17: Repaired and live-proved agency/apartment scraping, added exact per-run UI audit persistence and zero-result failure, migrated Railway leads/notes/events losslessly to Supabase, removed embedded legacy credentials, and verified the dashboard API reports all 428 merged leads.
- 2026-07-17: Reconciled the remaining Railway application tables into Supabase and restored the Railway users and scraper history; this keeps the frontend's historic run view consistent after the Vercel API base URL is switched.
- 2026-07-27: Shipped and browser-verified the transaction-pooling and worker status-persistence repair; production run `5` proved non-zero user-visible output and run `1` no longer remains stuck.
- 2026-07-27: Repaired the dashboard/outreach frontend contracts, restored the verified missing historical data additively, and browser-verified every top-level route against the fixed local stack.
- 2026-07-27: Replaced long-lived scraper database sessions with short-lived retryable transactions after production run `41` exposed an idle transaction-pool disconnect.
- 2026-07-27: Production run `45` verified the final worker image end to end with accurate UI audit counts and no Cloud Run errors.
